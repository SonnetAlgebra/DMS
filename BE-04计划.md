BE-04 执行计划
交付物
app/routers/metrics.py - /api/v1/metrics 路由（获取指标列表）
app/routers/data.py - /api/v1/metrics/{id}/data 路由（时序数据查询）
app/services/anomaly.py - Z-Score 异常检测服务
app/schemas/data.py - Pydantic 模式
app/main.py 更新 - 注册 metrics 和 data 路由
实施步骤
步骤1：创建异常检测服务（services/anomaly.py）
Python
复制
import numpy as np
from typing import List, Dict, Optional


class AnomalyService:
    """异常检测服务 - Z-Score 算法"""

    def detect(self, values: List[Optional[float]], threshold: float = 3.0) -> Dict:
        """
        执行 Z-Score 异常检测

        参数:
            values: 数值列表（含 None 表示缺失值）
            threshold: Z-Score 阈值，默认 3.0

        返回: {mean, std, z_scores, error}
        """
        # 1. 过滤有效值
        valid_values = [v for v in values if v is not None]

        # 2. 边界判断：有效数据点 < 2
        if len(valid_values) < 2:
            return {
                "error": {
                    "code": "INSUFFICIENT_DATA",
                    "message": "有效数据点不足2个"
                }
            }

        # 3. 计算统计量
        mean = float(np.mean(valid_values))
        std = float(np.std(valid_values))

        # 4. 除零保护：标准差为 0 时 z_scores 返回 0
        if std == 0:
            z_scores = [0.0] * len(values)
        else:
            z_scores = []
            for v in values:
                if v is None:
                    z_scores.append(0.0)
                else:
                    z_scores.append(abs((v - mean) / std))

        return {
            "mean": mean,
            "std": std,
            "z_scores": z_scores
        }
步骤2：创建 Pydantic 模式（schemas/data.py）
Python
复制
from pydantic import BaseModel
from typing import List, Optional


class DataPoint(BaseModel):
    """时序数据点"""
    timestamp: str
    value: Optional[float]        # NULL 表示缺失值
    is_anomaly: bool = False
    z_score: float = 0.0


class DataResponse(BaseModel):
    """时序数据响应"""
    data: List[DataPoint]
    mean: Optional[float] = None
    std: Optional[float] = None
    limit: int
    offset: int
    total: int


class MetricInfo(BaseModel):
    """指标信息"""
    id: int
    name: str
    description: Optional[str] = None
    unit: Optional[str] = None


class MetricsListResponse(BaseModel):
    """指标列表响应"""
    metrics: List[MetricInfo]
    total: int
步骤3：实现指标列表路由（routers/metrics.py）
Python
复制
from fastapi import APIRouter
from app.schemas.data import MetricsListResponse, MetricInfo
from app.config import settings
import sqlite3

router = APIRouter()
DB_PATH = settings.database_path


@router.get("/", response_model=MetricsListResponse)
async def get_metrics():
    """
    获取所有指标列表

    接口路径: /api/v1/metrics
    """
    conn = sqlite3.connect(DB_PATH)
    cursor = conn.cursor()

    cursor.execute(
        "SELECT id, name, description, unit FROM metrics ORDER BY id"
    )
    rows = cursor.fetchall()

    conn.close()

    metrics = [
        MetricInfo(
            id=row[0],
            name=row[1],
            description=row[2],
            unit=row[3]
        )
        for row in rows
    ]

    return MetricsListResponse(
        metrics=metrics,
        total=len(metrics)
    )
步骤4：实现时序数据查询路由（routers/data.py）
Python
复制
from fastapi import APIRouter, Query, HTTPException
from app.schemas.data import DataResponse, DataPoint
from app.services.anomaly import AnomalyService
from app.config import settings
import sqlite3

router = APIRouter()
anomaly_service = AnomalyService()
DB_PATH = settings.database_path


@router.get("/{metric_id}/data", response_model=DataResponse)
async def get_metric_data(
    metric_id: int,
    threshold: float = Query(3.0, description="Z-Score 阈值", ge=0.1, le=10.0),
    limit: int = Query(1000, description="返回条数限制", ge=1, le=5000),
    offset: int = Query(0, description="偏移量", ge=0)
):
    """
    获取指定指标的时序数据（含动态异常判定）

    接口路径: /api/v1/metrics/{metric_id}/data

    参数:
        metric_id: 指标 ID
        threshold: Z-Score 阈值，默认 3.0
        limit: 返回条数限制，最大 5000
        offset: 偏移量，用于分页
    """
    conn = sqlite3.connect(DB_PATH)
    cursor = conn.cursor()

    # 1. 检查指标是否存在
    cursor.execute("SELECT id FROM metrics WHERE id = ?", (metric_id,))
    if cursor.fetchone() is None:
        conn.close()
        raise HTTPException(status_code=404, detail="指标不存在")

    # 2. 查询总数
    cursor.execute(
        "SELECT COUNT(*) FROM time_series WHERE metric_id = ?",
        (metric_id,)
    )
    total = cursor.fetchone()[0]

    # 3. 查询时序数据（带分页）
    cursor.execute(
        """
        SELECT timestamp, value
        FROM time_series
        WHERE metric_id = ?
        ORDER BY timestamp
        LIMIT ? OFFSET ?
        """,
        (metric_id, limit, offset)
    )
    rows = cursor.fetchall()

    conn.close()

    # 4. 提取数值
    values = [row[1] for row in rows]

    # 5. 执行异常检测
    result = anomaly_service.detect(values, threshold)

    # 6. 处理错误
    if "error" in result:
        raise HTTPException(
            status_code=400,
            detail=result["error"]
        )

    # 7. 构建响应数据
    data_points = []
    for i, row in enumerate(rows):
        is_anomaly = result["z_scores"][i] > threshold if result["std"] > 0 else False
        data_points.append(
            DataPoint(
                timestamp=row[0],
                value=row[1],
                is_anomaly=is_anomaly,
                z_score=result["z_scores"][i]
            )
        )

    return DataResponse(
        data=data_points,
        mean=result["mean"],
        std=result["std"],
        limit=limit,
        offset=offset,
        total=total
    )
步骤5：更新主应用（main.py）
Python
复制
# 注册路由
from app.routers import upload, metrics, data

app.include_router(upload.router, prefix="/api/v1/data", tags=["data"])
app.include_router(metrics.router, prefix="/api/v1/metrics", tags=["metrics"])
app.include_router(data.router, prefix="/api/v1/metrics", tags=["data"])
关键约束
除零保护强制：std=0 时 z_score 返回 0，不可抛异常
有效数据点<2时返回错误码 INSUFFICIENT_DATA
limit=5000 硬限制，超出请求返回 400
offset 超过总数时返回空数组，不报错
动态计算不存储：is_anomaly 和 z_score 仅返回，不入库
验收标准
[ ] GET /api/v1/metrics 返回指标列表
[ ] GET /api/v1/metrics/{id}/data 返回时序数据（含 is_anomaly、z_score）
[ ] limit=5000 限制生效，超过返回 400
[ ] offset 超过总数返回空数组，不报错
[ ] 有效数据点<2 返回错误码 INSUFFICIENT_DATA
[ ] std=0 时 z_score 返回 0，不抛异常
[ ] threshold 参数可配置，默认 3.0
测试用例
bash
复制
# 获取指标列表
curl -s "http://127.0.0.1:8000/api/v1/metrics"

# 获取时序数据
curl -s "http://127.0.0.1:8000/api/v1/metrics/1/data?threshold=3.0&limit=100&offset=0"

# 测试 limit 超限
curl -s "http://127.0.0.1:8000/api/v1/metrics/1/data?limit=6000" | jq .

# 测试不存在指标
curl -s "http://127.0.0.1:8000/api/v1/metrics/999/data" | jq .
预期响应
JSON
复制
{
  "data": [
    {
      "timestamp": "2024-01-01",
      "value": 1000,
      "is_anomaly": false,
      "z_score": 0.5
    }
  ],
  "mean": 1100.0,
  "std": 200.0,
  "limit": 100,
  "offset": 0,
  "total": 1000
}
